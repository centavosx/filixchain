{
  "name": "@ph-blockchain/transformer",
  "version": "0.0.1",
  "description": "",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "rm -rf dist tsconfig.tsbuildinfo && tsc --build"
  },
  "dependencies": {
    "level": "^9.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
